spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tuzigiri_dev
    username: root
    password: password
    driver-class-name: com.mysql.jdbc.Driver
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
doma:
  dialect: mysql
